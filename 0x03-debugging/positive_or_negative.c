#include "main.h"

/**
 * main - Determines the number if it is positive, negative, or zero.
 *
 * Return: Always 0.
*/
void positive_or_negative(int i)
{

if (i < 0)
{
printf("%d is %s\n", i,"negative");
}
else if (1 > 0)
{
printf("%d is %s\n", i,"positive");
}
else 
{
printf("%d is %s\n", i, "zero");
}
return;

}
